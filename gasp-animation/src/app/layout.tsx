import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'GASP Scroll Trigger Animations',
  description: 'Dive into the enchanting world of GASP Scroll Trigger Animations! Explore the mesmerizing realm where web content gracefully unfolds with seamless scrolling.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
